- name: Convertir string a lista de paquetes
  set_fact:
        paquetes: "{{ paquete.split(',') | map('trim') | list }}"

- name: Instalar paquetes indicados
  become: yes
  ansible.builtin.package:
    name: "{{ paquete }}"
    state: present